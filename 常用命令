#申请“www队列”节点：

bash
csub -q www -I -n 1 /bin/bash
执行后耐心等待片刻，一旦命令提示符的前缀从 [xxx@hw90 ...] 变成了其他主机名（例如 [xxx@cXXXXXX ...]），就说明你成功登录到了计算节点。

在计算节点上配置环境：

bash
# 1. 重新加载你的 conda
source ~/miniconda3/etc/profile.d/conda.sh

# 2. 配置清华源（按文档第39页示例操作）
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/free/
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main/
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/conda-forge/
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/bioconda/
conda config --set show_channel_urls yes

# 3. 创建环境
conda create --prefix ~/CUT_Tag_env python=3.9 -y
这应该就能成功了。创建完成后，可以输入 exit 退出节点，回到 login 队列。

💎 总结
简单来说，就是用“www队列”来解决网络问题，用自己安装的 Miniconda 来保证稳定可控。这两个办法可以解决你遇到的大部分问题。

#安装软件
csub -q www -I -n 1 /bin/bash  #申请节点
source ~/miniconda3/etc/profile.d/conda.sh  #重新加载env
conda activate ~/CUT_Tag_env #激活env
conda install -c bioconda -y fastp  #安装软件
