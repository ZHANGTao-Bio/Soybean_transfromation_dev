如果登录节点无法访问外网，请参考文档第10页：使用 www 队列（虚拟机）来下载。您可以提交一个交互式任务：

bash
# 申请一个 www 队列的交互式节点
csub -q www -I -n 1 /bin/bash
如果 /bin/bash 成功执行，你的终端会直接切换到那个请求到的计算节点上，命令提示符也会变成节点主机名（例如 [yzwl_zhangtao@cXXXXXX ~]$）。
此时，你就登录到了该计算节点，可以自由地输入和执行任何命令，比如 wget、ls、top 等。这就是“交互式作业”的体验。
# 然后在弹出的终端中执行上面的 wget 命令
